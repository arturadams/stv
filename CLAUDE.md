# Project instructions

## Versioning

Bump `version` in `package.json` in the same commit as any functional
change (gameplay, cards, relics, balance, UI, bug fixes). See the
"Versioning" section in `README.md` for the semver rules. Skip the bump
only for docs-only or tooling-only changes.
